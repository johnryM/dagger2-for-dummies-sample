<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <TextView
        android:id="@+id/textTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="48dp"
        tools:text="Sample"/>

    <TextView
        android:id="@+id/textTitleArguement"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        app:layout_constraintLeft_toLeftOf="@id/textTitle"
        app:layout_constraintRight_toRightOf="@id/textTitle"
        app:layout_constraintTop_toBottomOf="@id/textTitle"
        android:layout_marginTop="16dp"
        tools:text="Sample just below"/>

    <Button
        android:id="@+id/activityButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:text="@string/go_to_second_activity"
        app:layout_constraintEnd_toEndOf="@+id/textTitleArguement"
        app:layout_constraintStart_toStartOf="@+id/textTitleArguement"
        app:layout_constraintTop_toBottomOf="@+id/textTitleArguement"
        android:onClick="openSecondActivity"/>

</android.support.constraint.ConstraintLayout>